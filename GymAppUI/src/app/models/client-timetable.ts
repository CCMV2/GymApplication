export class ClientTimetable {
    client: string;
    timetable: number;

    constructor(client: string, timetable: number) {
        this.client = client;
        this.timetable = timetable;
    }
}